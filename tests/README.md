# Tests

Run:

```bash
pytest -q
```

The tests validate the current repository datasets before cloud loading.

Add Azure integration tests only when the corresponding cloud resources are actually available.
