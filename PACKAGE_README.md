# Retail Data Engineering — Master's Upgrade Package

This package is designed to raise the repository's portfolio quality from the current state by adding visible engineering artifacts around the existing Datasets and Sales_Project.ipynb.

## Upload/merge

Keep the existing:
- `Datasets/`
- `Sales_Project.ipynb`

Add the folders/files in this package.

## Critical rule

The Azure screenshots, pipeline results, performance numbers, data-quality percentages and Power BI dashboard must come from the actual environment. This package does not fabricate evidence.

The current repository publicly describes an Azure Data Factory + ADLS + Databricks + Azure SQL architecture, while the visible repository currently contains `Datasets/`, `README.md` and `Sales_Project.ipynb`. citeturn1view0turn2view0

The package therefore focuses on adding the missing implementation/documentation artifacts without pretending cloud resources have been executed.
